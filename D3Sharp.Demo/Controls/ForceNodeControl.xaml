<UserControl x:Class="D3Sharp.Demo.Controls.ForceNodeControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:D3Sharp.Demo.Controls"
             mc:Ignorable="d" ToolTip="{Binding Name}"
             d:DesignHeight="100" d:DesignWidth="100" SizeChanged="UserControl_SizeChanged">
    <Grid>
        <Rectangle x:Name="rect"  Fill="{Binding Brush}"
                   Stroke="LightGray"/>
        <Ellipse x:Name="ellipse" Fill="{Binding Brush}"
                 StrokeThickness="2" Stroke="#21212C"/>
        <TextBlock Margin="7,5" x:Name="textBlock" 
                   FontSize="11" FontFamily="Consolas" TextWrapping="Wrap"
                   Text="{Binding Name}" Foreground="White"/>
    </Grid>
</UserControl>
